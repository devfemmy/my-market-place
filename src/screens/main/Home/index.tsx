import React from 'react';
import {SafeAreaView, Text} from '../../../components/common';

export const Home = (): JSX.Element => {
  return (
    <SafeAreaView>
      <Text text="Home" />
    </SafeAreaView>
  );
};
